<h1>Products from the Amazon API</h1>
<% if @products.any? %>
    <% @products.each do |product| %>
        <div class="product">
          <%= link_to image_tag(product.image_url), product.url %>
          <%= link_to product.name, product.url %>
          <%= link_to "Reviews", product.link %>
         abc  <%= product.price %>
        </div>
    <% end %>
<% end %>